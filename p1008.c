#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main() {
    char num[9][2] = { "1", "2", "3", "4", "5", "6", "7", "8", "9"};
    int n,i,j,k;

    for (i = 0; i < 3;i++){
        
    }
        n = atoi(strcat(num[3], strcat(num[0], num[1])));
    printf("Test:%d\n", n);
    return 0;
}