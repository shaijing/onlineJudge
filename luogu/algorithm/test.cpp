#include <iostream>
int main() {
    // float a = 0.1;
    // printf("%d\n", int(2 - a * a * 100));
    // printf("%.10f\n", 2 - a * a * 100);

    int a = 3, b = 4, c = 5;
    printf("%d", (a < b || b > c || a > b));
    return 0;
}